import { Injectable } from '@nestjs/common';

/**
 * The AppService class is a service provider in the NestJS application.
 * It is decorated with the @Injectable() decorator, which marks it as a
 * provider that can be injected into other components, such as controllers.
 */
@Injectable()
export class AppService {
  // Currently, the AppService class does not contain any methods or properties.
  // It can be expanded in the future to include business logic and utility functions
  // that can be used across the application.
}
